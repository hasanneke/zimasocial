package com.zima.zimasocial.context.communication.infastructure;

public class UserEntityRecipientConverter {
//    public static RecipientDomain toRecipient(Recipient user) {
//        return new RecipientDomain(user.getId(), user.getSlug(), user.getDeviceTokens().stream().map(e->new DeviceToken(e.getId(), e.getToken(), e.getUserId())).collect(Collectors.toSet()) , user.getEmail(), user.getFullName(), user.getAvatarFileName(), user.getIsPrivate());
//    }

//    public static RecipientDomain toRecipient(Author author) {
//        return new RecipientDomain(new RecipientId(author.getId().getValue()), author.getSlug(), author.getDeviceTokens().stream().map(e->new DeviceToken(e.getId(), e.getToken(), e.getUserId())).collect(Collectors.toSet()) , author.getEmail(), author.getFullName(), author.getAvatarFileName(), author.getIsPrivate());
//    }
}
