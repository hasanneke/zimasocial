package com.zimaberlin.zimasocial.repository;

import com.zimaberlin.zimasocial.entity.postReport.PostReportReason;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PostReportReasonRepository extends JpaRepository<PostReportReason, Long> {
}
